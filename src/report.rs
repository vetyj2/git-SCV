//! 사람용 리포트.
//! 마지막 무실행 문장은 model::NO_EXEC_SENTENCE 상수를 쓴다.

use crate::model::RunData;

pub fn render(data: &RunData) -> String {
    let _ = data;
    todo!("report rendering is not implemented yet")
}
